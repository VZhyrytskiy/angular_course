export interface ConfigModel {
    id: string;
    login: string;
    email: string;
}

export interface ConstantLiteral {
    app: string;
    ver: string;
    API_URL: string;
    applicationid: number;
    clientid: string;
    environment: boolean;
}
